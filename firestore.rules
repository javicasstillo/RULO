rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Solo usuarios logueados (vos y quien más crees en Firebase Auth)
    // pueden leer y escribir datos del negocio.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
